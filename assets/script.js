const hours = [9, 10, 11, 12, 13, 14, 15, 16, 17];
var now = new Date();
var currentHour = now.getHours();

const key = "Current Score";

const localStorageCurrentScore = localStorage.getItem(key);


// if (localStorageCurrentScore = null ) {
  
}
// if (localStorageCurrentScore = null) {
// currentScore = JSON.parse(localStorageSchedule);
// }
// const clock = document.getElementById("clock");
// clock.innerHTML = now;

